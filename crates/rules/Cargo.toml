[package]
name = "hs-rules"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
ron = "0.8"
thiserror = "2"
rand = "0.8"

[dev-dependencies]
tempfile = "3"
